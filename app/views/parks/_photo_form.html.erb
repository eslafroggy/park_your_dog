<%= form_for [@park, Photo.new], :html => {:multipart => true} do |f| %>
  <p>
    <label>Add A Photo:</label>
    <%= f.text_field :remote_photo_url %>
  </p>
  <p>
  	<%= f.submit "Add Photo" %> 
  </p>
	
<% end %>